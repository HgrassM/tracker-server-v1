package client

import (
	"net"
	"log"
	"sync"
)

func ClientRoutine(addr string, map_ptr, counter_ptr) {
	//TODO handle the connection, add counter based on connection status and wait for messages
}
